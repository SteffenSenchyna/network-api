apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "network-api.fullname" . }}-test-connection"
  labels:
    {{- include "network-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "network-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
